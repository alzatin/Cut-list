{
  "atomType": "Molecule",
  "x": 0,
  "y": 0,
  "uniqueID": "id-1",
  "name": "Cut-list",
  "ioValues": [
    {
      "name": "Parts",
      "ioValue": "__GEOMETRY_INPUT__"
    }
  ],
  "topLevel": true,
  "allAtoms": [
    {
      "atomType": "Output",
      "x": 0.98,
      "y": 0.5,
      "uniqueID": "id-2",
      "ioValues": [
        {
          "name": "number or geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ]
    },
    {
      "atomType": "Input",
      "x": 0.0264,
      "y": 0.2865853658536585,
      "uniqueID": "id-4",
      "name": "Parts",
      "type": "geometry"
    },
    {
      "atomType": "Rectangle",
      "x": 0.06089193825042882,
      "y": 0.7652439024390244,
      "uniqueID": "id-13",
      "ioValues": [
        {
          "name": "x length",
          "ioValue": 85,
          "currentEquation": "85"
        },
        {
          "name": "y length",
          "ioValue": 7,
          "currentEquation": "7"
        }
      ]
    },
    {
      "atomType": "Extrude",
      "x": 0.20154373927958835,
      "y": 0.35060975609756095,
      "uniqueID": "id-17",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 1
        }
      ]
    },
    {
      "atomType": "Assembly",
      "x": 0.5042881646655232,
      "y": 0.27439024390243905,
      "uniqueID": "id-21",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 2",
          "ioValue": null
        },
        {
          "name": "Shape 3",
          "ioValue": null
        },
        {
          "name": "Shape 4",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Move",
      "x": 0.35334476843910806,
      "y": 0.44817073170731714,
      "uniqueID": "id-25",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 0
        },
        {
          "name": "yDist",
          "ioValue": 20
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    },
    {
      "atomType": "Move",
      "x": 0.23670668953687823,
      "y": 0.5823170731707317,
      "uniqueID": "id-31",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 0
        },
        {
          "name": "yDist",
          "ioValue": 40
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    },
    {
      "atomType": "Readme",
      "x": 0.7615780445969125,
      "y": 0.323170731707317,
      "uniqueID": "id-41",
      "name": "README",
      "ioValues": [
        {
          "name": "value",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ],
      "readMeText": "CUTLIST",
      "global": true
    },
    {
      "atomType": "Rectangle",
      "x": 0.1672384219554031,
      "y": 0.8231707317073171,
      "uniqueID": "id-74",
      "ioValues": [
        {
          "name": "x length",
          "ioValue": 100
        },
        {
          "name": "y length",
          "ioValue": 100
        }
      ]
    },
    {
      "atomType": "Extrude",
      "x": 0.3181818181818182,
      "y": 0.7225609756097561,
      "uniqueID": "id-84",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 20,
          "currentEquation": "20"
        }
      ]
    },
    {
      "atomType": "Tag",
      "x": 0.34991423670668953,
      "y": 0.2103658536585366,
      "uniqueID": "id-90",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ],
      "tags": [
        ""
      ]
    },
    {
      "atomType": "Move",
      "x": 0.4142367066895368,
      "y": 0.6189024390243902,
      "uniqueID": "id-93",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 20
        },
        {
          "name": "yDist",
          "ioValue": -300
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    },
    {
      "atomType": "Code",
      "x": 0.6475128644939966,
      "y": 0.24695121951219512,
      "uniqueID": "id-206",
      "ioValues": [
        {
          "name": "partAssembly",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "roundTo",
          "ioValue": 2,
          "currentEquation": "2"
        },
        {
          "name": "sortBy",
          "ioValue": "quantity",
          "currentEquation": "quantity"
        }
      ],
      "interpreterVersion": 1,
      "code": "/**\n * Generates a cut list from all geometries in an assembly,\n * grouped by dimension and quantity.\n */\nfunction run(\n  partAssembly: Assembly | Assembly[] | undefined = undefined,\n  roundTo: number = 2,\n  sortBy: string = \"quantity\",\n): string {\n  if (!partAssembly) {\n    return \"No assembly connected. Please connect a geometry input.\";\n  }\n\n  // Normalize input to array\n  const assemblies = Array.isArray(partAssembly)\n    ? partAssembly\n    : [partAssembly];\n  // Extract all individual parts with their bounds\n  const grouped: Record<string, number> = {};\n  console.log(assemblies[0].geometry)\n  assemblies[0].geometry.forEach((assembly) => {\n    if (!assembly || !assembly.geometry) return;\n\n    try {\n      // Access boundingBox property on the geometry\n      console.log(assembly.geometry)\n      const bbox = assembly.geometry.boundingBox;\n      if (!bbox || !bbox.bounds) {\n        console.warn(\"No bounding box found for part\");\n        return;\n      }\n\n      // Extract dimensions from bounds\n      const minPoint = bbox.bounds[0];\n      const maxPoint = bbox.bounds[1];\n\n      const x = Math.abs(maxPoint[0] - minPoint[0]);\n      const y = Math.abs(maxPoint[1] - minPoint[1]);\n      const z = Math.abs(maxPoint[2] - minPoint[2]);\n\n      // Sort dimensions for consistency\n      const dims = [x, y, z]\n        .sort((a, b) => b - a)\n        .map((d) => d.toFixed(roundTo))\n        .join(\" × \");\n\n      grouped[dims] = (grouped[dims] || 0) + 1;\n    } catch (e) {\n      console.warn(\"Could not extract bounds for part:\", e);\n    }\n  });\n\n  if (Object.keys(grouped).length === 0) {\n    return \"No valid geometries found in assembly.\";\n  }\n\n  // Sort results\n  const sorted = Object.entries(grouped).sort((a, b) => {\n    if (sortBy === \"quantity\") {\n      return b[1] - a[1]; // Most common first\n    } else {\n      return a[0].localeCompare(b[0]); // Alphabetical\n    }\n  });\n\n  // Generate markdown table\n  let table =\n    \"| Dimensions (X × Y × Z) | Quantity |\\n|------------------------|----------|\\n\";\n  let totalParts = 0;\n\n  sorted.forEach(([dims, qty]) => {\n    table += `| ${dims} mm | ${qty} |\\n`;\n    totalParts += qty;\n  });\n\n  table += `\\n**Total Parts:** ${totalParts}`;\n  console.log(table)\n  return table;\n}",
      "compiledCode": "\"use strict\";\n/**\n * Generates a cut list from all geometries in an assembly,\n * grouped by dimension and quantity.\n */\nfunction run(partAssembly = undefined, roundTo = 2, sortBy = \"quantity\") {\n    if (!partAssembly) {\n        return \"No assembly connected. Please connect a geometry input.\";\n    }\n    // Normalize input to array\n    const assemblies = Array.isArray(partAssembly)\n        ? partAssembly\n        : [partAssembly];\n    // Extract all individual parts with their bounds\n    const grouped = {};\n    console.log(assemblies[0].geometry);\n    assemblies[0].geometry.forEach((assembly) => {\n        if (!assembly || !assembly.geometry)\n            return;\n        try {\n            // Access boundingBox property on the geometry\n            console.log(assembly.geometry);\n            const bbox = assembly.geometry.boundingBox;\n            if (!bbox || !bbox.bounds) {\n                console.warn(\"No bounding box found for part\");\n                return;\n            }\n            // Extract dimensions from bounds\n            const minPoint = bbox.bounds[0];\n            const maxPoint = bbox.bounds[1];\n            const x = Math.abs(maxPoint[0] - minPoint[0]);\n            const y = Math.abs(maxPoint[1] - minPoint[1]);\n            const z = Math.abs(maxPoint[2] - minPoint[2]);\n            // Sort dimensions for consistency\n            const dims = [x, y, z]\n                .sort((a, b) => b - a)\n                .map((d) => d.toFixed(roundTo))\n                .join(\" × \");\n            grouped[dims] = (grouped[dims] || 0) + 1;\n        }\n        catch (e) {\n            console.warn(\"Could not extract bounds for part:\", e);\n        }\n    });\n    if (Object.keys(grouped).length === 0) {\n        return \"No valid geometries found in assembly.\";\n    }\n    // Sort results\n    const sorted = Object.entries(grouped).sort((a, b) => {\n        if (sortBy === \"quantity\") {\n            return b[1] - a[1]; // Most common first\n        }\n        else {\n            return a[0].localeCompare(b[0]); // Alphabetical\n        }\n    });\n    // Generate markdown table\n    let table = \"| Dimensions (X × Y × Z) | Quantity |\\n|------------------------|----------|\\n\";\n    let totalParts = 0;\n    sorted.forEach(([dims, qty]) => {\n        table += `| ${dims} mm | ${qty} |\\n`;\n        totalParts += qty;\n    });\n    table += `\\n**Total Parts:** ${totalParts}`;\n    console.log(table);\n    return table;\n}\n"
    },
    {
      "atomType": "Assembly",
      "x": 0.6269296740994854,
      "y": 0.48780487804878053,
      "uniqueID": "id-219",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 2",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Code",
      "x": 0.83704974271012,
      "y": 0.48780487804878053,
      "uniqueID": "id-223",
      "ioValues": [
        {
          "name": "partAssembly",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "roundTo",
          "ioValue": 2
        },
        {
          "name": "sortBy",
          "ioValue": "quantity",
          "currentEquation": "quantity"
        }
      ],
      "interpreterVersion": 1,
      "code": "/**\n * Generates a cut list from all geometries in an assembly,\n * grouped by dimension and quantity. Handles deeply nested assemblies.\n */\nfunction run(\n  partAssembly: Assembly | Assembly[] | undefined = undefined,\n  roundTo: number = 2,\n  sortBy: string = \"quantity\",\n): string {\n  if (!partAssembly) {\n    return \"No assembly connected. Please connect a geometry input.\";\n  }\n\n  // Normalize input to array\n  const assemblies = Array.isArray(partAssembly)\n    ? partAssembly\n    : [partAssembly];\n\n  // Extract all individual parts with their bounds\n  const grouped: Record<string, number> = {};\n\n  // Recursively traverse nested assemblies to find all replicad geometry\n  function extractGeometries(obj: any): any[] {\n    if (!obj) return [];\n\n    // Skip strings\n    if (typeof obj === \"string\") {\n      return [];\n    }\n\n    // If this object has a boundingBox property, it's actual replicad geometry\n    if (obj.boundingBox && typeof obj.boundingBox === \"object\") {\n      return [obj];\n    }\n\n    // If this is an assembly (has geometry property), recurse into it\n    if (obj.geometry) {\n      const results: any[] = [];\n\n      // geometry can be an array of items or a single item\n      if (Array.isArray(obj.geometry)) {\n        for (const item of obj.geometry) {\n          results.push(...extractGeometries(item));\n        }\n      } else {\n        // Single geometry item\n        results.push(...extractGeometries(obj.geometry));\n      }\n      return results;\n    }\n\n    // If it's an array at top level, recurse into each element\n    if (Array.isArray(obj)) {\n      const results: any[] = [];\n      for (const item of obj) {\n        results.push(...extractGeometries(item));\n      }\n      return results;\n    }\n\n    return [];\n  }\n\n  // Process all assemblies\n  assemblies.forEach((assembly) => {\n    if (!assembly) return;\n\n    const geometries = extractGeometries(assembly.geometry);\n\n    geometries.forEach((geometry) => {\n      try {\n        const bbox = geometry.boundingBox;\n\n        // Validate boundingBox structure\n        if (\n          !bbox ||\n          typeof bbox !== \"object\" ||\n          !Array.isArray(bbox.bounds) ||\n          bbox.bounds.length !== 2\n        ) {\n          return;\n        }\n\n        // Extract dimensions from bounds\n        const minPoint = bbox.bounds[0];\n        const maxPoint = bbox.bounds[1];\n\n        if (\n          !Array.isArray(minPoint) ||\n          !Array.isArray(maxPoint) ||\n          minPoint.length < 3 ||\n          maxPoint.length < 3\n        ) {\n          return;\n        }\n\n        const x = Math.abs(maxPoint[0] - minPoint[0]);\n        const y = Math.abs(maxPoint[1] - minPoint[1]);\n        const z = Math.abs(maxPoint[2] - minPoint[2]);\n\n        // Sort dimensions for consistency\n        const dims = [x, y, z]\n          .sort((a, b) => b - a)\n          .map((d) => d.toFixed(roundTo))\n          .join(\" × \");\n\n        grouped[dims] = (grouped[dims] || 0) + 1;\n      } catch (e) {\n        console.warn(\"Could not extract bounds for geometry:\", e);\n      }\n    });\n  });\n\n  if (Object.keys(grouped).length === 0) {\n    return \"No valid geometries found in assembly.\";\n  }\n\n  // Sort results\n  const sorted = Object.entries(grouped).sort((a, b) => {\n    if (sortBy === \"quantity\") {\n      return b[1] - a[1]; // Most common first\n    } else {\n      return a[0].localeCompare(b[0]); // Alphabetical\n    }\n  });\n\n  // Generate markdown table\n  let table =\n    \"| Dimensions (X × Y × Z) | Quantity |\\n|------------------------|----------|\\n\";\n  let totalParts = 0;\n\n  sorted.forEach(([dims, qty]) => {\n    table += `| ${dims} mm | ${qty} |\\n`;\n    totalParts += qty;\n  });\n\n  table += `\\n**Total Parts:** ${totalParts}`;\n  console.log(table)\n  return table;\n}",
      "compiledCode": "\"use strict\";\n/**\n * Generates a cut list from all geometries in an assembly,\n * grouped by dimension and quantity. Handles deeply nested assemblies.\n */\nfunction run(partAssembly = undefined, roundTo = 2, sortBy = \"quantity\") {\n    if (!partAssembly) {\n        return \"No assembly connected. Please connect a geometry input.\";\n    }\n    // Normalize input to array\n    const assemblies = Array.isArray(partAssembly)\n        ? partAssembly\n        : [partAssembly];\n    // Extract all individual parts with their bounds\n    const grouped = {};\n    // Recursively traverse nested assemblies to find all replicad geometry\n    function extractGeometries(obj) {\n        if (!obj)\n            return [];\n        // Skip strings\n        if (typeof obj === \"string\") {\n            return [];\n        }\n        // If this object has a boundingBox property, it's actual replicad geometry\n        if (obj.boundingBox && typeof obj.boundingBox === \"object\") {\n            return [obj];\n        }\n        // If this is an assembly (has geometry property), recurse into it\n        if (obj.geometry) {\n            const results = [];\n            // geometry can be an array of items or a single item\n            if (Array.isArray(obj.geometry)) {\n                for (const item of obj.geometry) {\n                    results.push(...extractGeometries(item));\n                }\n            }\n            else {\n                // Single geometry item\n                results.push(...extractGeometries(obj.geometry));\n            }\n            return results;\n        }\n        // If it's an array at top level, recurse into each element\n        if (Array.isArray(obj)) {\n            const results = [];\n            for (const item of obj) {\n                results.push(...extractGeometries(item));\n            }\n            return results;\n        }\n        return [];\n    }\n    // Process all assemblies\n    assemblies.forEach((assembly) => {\n        if (!assembly)\n            return;\n        const geometries = extractGeometries(assembly.geometry);\n        geometries.forEach((geometry) => {\n            try {\n                const bbox = geometry.boundingBox;\n                // Validate boundingBox structure\n                if (!bbox ||\n                    typeof bbox !== \"object\" ||\n                    !Array.isArray(bbox.bounds) ||\n                    bbox.bounds.length !== 2) {\n                    return;\n                }\n                // Extract dimensions from bounds\n                const minPoint = bbox.bounds[0];\n                const maxPoint = bbox.bounds[1];\n                if (!Array.isArray(minPoint) ||\n                    !Array.isArray(maxPoint) ||\n                    minPoint.length < 3 ||\n                    maxPoint.length < 3) {\n                    return;\n                }\n                const x = Math.abs(maxPoint[0] - minPoint[0]);\n                const y = Math.abs(maxPoint[1] - minPoint[1]);\n                const z = Math.abs(maxPoint[2] - minPoint[2]);\n                // Sort dimensions for consistency\n                const dims = [x, y, z]\n                    .sort((a, b) => b - a)\n                    .map((d) => d.toFixed(roundTo))\n                    .join(\" × \");\n                grouped[dims] = (grouped[dims] || 0) + 1;\n            }\n            catch (e) {\n                console.warn(\"Could not extract bounds for geometry:\", e);\n            }\n        });\n    });\n    if (Object.keys(grouped).length === 0) {\n        return \"No valid geometries found in assembly.\";\n    }\n    // Sort results\n    const sorted = Object.entries(grouped).sort((a, b) => {\n        if (sortBy === \"quantity\") {\n            return b[1] - a[1]; // Most common first\n        }\n        else {\n            return a[0].localeCompare(b[0]); // Alphabetical\n        }\n    });\n    // Generate markdown table\n    let table = \"| Dimensions (X × Y × Z) | Quantity |\\n|------------------------|----------|\\n\";\n    let totalParts = 0;\n    sorted.forEach(([dims, qty]) => {\n        table += `| ${dims} mm | ${qty} |\\n`;\n        totalParts += qty;\n    });\n    table += `\\n**Total Parts:** ${totalParts}`;\n    console.log(table);\n    return table;\n}\n"
    },
    {
      "atomType": "Assembly",
      "x": 0.5300171526586621,
      "y": 0.6676829268292683,
      "uniqueID": "id-237",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 4",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Move",
      "x": 0.4425385934819897,
      "y": 0.8323170731707318,
      "uniqueID": "id-242",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 200,
          "currentEquation": "200"
        },
        {
          "name": "yDist",
          "ioValue": -300
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    }
  ],
  "allConnectors": [
    {
      "ap2Name": "partAssembly",
      "ap1ID": "id-4",
      "ap2ID": "id-223"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-13",
      "ap2ID": "id-17"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-17",
      "ap2ID": "id-25"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-17",
      "ap2ID": "id-31"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-17",
      "ap2ID": "id-90"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-21",
      "ap2ID": "id-219"
    },
    {
      "ap2Name": "partAssembly",
      "ap1ID": "id-21",
      "ap2ID": "id-206"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-25",
      "ap2ID": "id-21"
    },
    {
      "ap2Name": "Shape 3",
      "ap1ID": "id-31",
      "ap2ID": "id-21"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-74",
      "ap2ID": "id-84"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-84",
      "ap2ID": "id-93"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-84",
      "ap2ID": "id-242"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-90",
      "ap2ID": "id-21"
    },
    {
      "ap2Name": "Shape 4",
      "ap1ID": "id-93",
      "ap2ID": "id-21"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-93",
      "ap2ID": "id-237"
    },
    {
      "ap2Name": "number or geometry",
      "ap1ID": "id-223",
      "ap2ID": "id-2"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-237",
      "ap2ID": "id-219"
    },
    {
      "ap2Name": "Shape 4",
      "ap1ID": "id-242",
      "ap2ID": "id-237"
    }
  ],
  "unitsKey": "Unitless",
  "fileTypeVersion": 1,
  "filetypeVersion": 1
}